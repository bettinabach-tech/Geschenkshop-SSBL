{
  "mcpServers": {
    "playwright": {
      "command": "cmd",
      "args": [
        "/c",
        "npx",
        "-y",
        "@playwright/mcp@0.0.80",
        "--browser",
        "msedge",
        "--isolated",
        "--allowed-origins",
        "http://localhost:4321"
      ]
    }
  }
}
