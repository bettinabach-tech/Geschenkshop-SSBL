// new-site.mjs — `npm run new -- <slug>`: legt eine neue Seite aus einem Brief an.
// PLATZHALTER: Die echte Umsetzung liefert Aufgabe 003 (docs/tasks/003-*.md).
console.error(
  "npm run new: noch nicht gebaut — siehe Aufgabe 003 in docs/tasks/.",
);
process.exit(1);
