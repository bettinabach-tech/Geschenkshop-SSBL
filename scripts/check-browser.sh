#!/usr/bin/env bash
# check-browser.sh — verify-Stufe "browser" (nur --deep): Lighthouse,
# 375-px-Test ohne horizontales Scrollen, Barrierefreiheit im echten Browser.
# PLATZHALTER: Die echte Prüfung richtet Aufgabe 004 ein. Bis dahin ist
# --deep bewusst RED — eine nicht eingerichtete Prüfung ist kein Bestehen.
echo "Browser-Prüfungen noch nicht eingerichtet — siehe Aufgabe 004 in docs/tasks/."
exit 1
